version: 1
app:
  name: optimize-molecule-backend
  entrypoint: backend/app.py
  dockerfile: backend/Dockerfile
  build:
    context: .
    dockerfile: backend/Dockerfile
  environment:
    - FLASK_ENV=production
  ports:
    - 5000
