# API Base URL (optional - defaults to /api for production nginx proxy)
# Only needed if backend is on different domain or for local development
# REACT_APP_API_BASE_URL=/api

# For development/testing with local backend:
# REACT_APP_API_BASE_URL=http://localhost:5000
